<div>
  <% @votes.each do |vote| %>
  <a>Votes placed by: <%= vote.find_pub(vote.voted_by).name %>
  <br>
  </a><font color="grey" > First vote: <%= vote.find_pub(vote.first).name %></font>
  <br>
  </a> <font color="grey" >  Second vote: <%= vote.find_pub(vote.second).name%></font><br>
  </a> <font color="grey" >  Third vote: <%= vote.find_pub(vote.third).name %></font>
  <br><hr>
  <% end %>
<br>
</div>

<a href="/votes/new">Add another vote</a><br>